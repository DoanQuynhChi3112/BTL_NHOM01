@model IEnumerable<BTLNHOM01.Models.DanhMucHang>

@{
    ViewData["Title"] = "Index";
}

<h2 class="w3-center w3-text-blue" style=" font-family: Times New Roman; font-size:50px "> Danh Mục Hàng </h2>
<p>
    @Html.ActionLink("Tạo Mới Dữ Liệu", "Create")
</p>
<table class="w3-table-all w3-hoverable " style="font-family: Times New Roman; font-size:20px">
    <tr class="w3-blue-grey">
        <th>
            <label>Mã sản phẩm</label>
        </th>
        <th>
            <label>Tên sản phẩm</label>
        </th>
        <th>
            <label>Đơn vị tính</label>
        </th>
        <th>
            <label>Số lượng</label>
        </th>
        <th>
            <label>Gía sản phẩm</label>
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.TenHang)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TenHang)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DonViTinh)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.SoLuong)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.GiaSP)
    </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaHang">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaHang">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaHang">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
